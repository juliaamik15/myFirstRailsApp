<div class="product-reviews">
    <% @comments.each do |comment| %>
        <div class="row">
            <hr>
            	<strong><%= comment.user.first_name %></strong> <small><em><%= "#{time_ago_in_words(comment.created_at)} ago" %></em></small>
            
            <br>
           		<div class="rated" data-score="<%= comment.rating %>"></div>
           	
            <br><%= comment.body %>
        </div>
    <% end %>
</div>